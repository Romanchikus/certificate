# PostgreSQL
# ----------docker-compose down -v---------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=certificate
POSTGRES_USER=certificate
POSTGRES_PASSWORD=certificate